当前TUI界面中状态概览模块占用空间过大，需要从主界面移除，改为在子页面呈现

换行处理有问题

合并流式输出、工具调用、历史消息，全部在主ui容器中显示

alt快捷键不生效

主页面选中文本后ctrl+C用于复制(终端应该能自己处理，不需要额外实现)
第一次按在状态栏显示提示"再次按Ctrl+C退出"，3s后消失。3s内再按则正常退出。
子页面中按ctrl+C直接回到主页面

esc用于清理输入框内容

需要实现虚拟滚动，支持主界面的主ui容器无限扩展，显示所有历史信息、流式输出、工具调用